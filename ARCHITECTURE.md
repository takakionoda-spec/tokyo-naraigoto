# アーキテクチャ比較検討（タスク1）

結論を先に：**Astro（静的生成）+ Cloudflare Pages + リポジトリ内JSON + GitHub Actions cron + Gemini Flash無料枠 + Formspree**

```
[GitHub Actions (10日毎cron)]
   └─ scripts/crawl.mjs を実行
        ├─ data/sources.json のURLを巡回・本文抽出
        ├─ Gemini Flash(無料枠)でカテゴリ判定・ニーズタグ付与・200字紹介文生成
        ├─ 重複排除して src/data/classrooms.json に追記
        └─ git commit & push
              └─ push を検知して Cloudflare Pages が自動ビルド・自動デプロイ
                    └─ 静的HTML配信（帯域無制限・0円）
[教室オーナー] → Formspree(無料50件/月) → メール受信（有料化・修正・削除依頼）
```

## 1. フロントエンド + ホスティング

| 候補 | 評価 | 理由 |
|---|---|---|
| 単一HTML + GitHub Pages | △ | 構築は最速だが、教室ごとの個別ページが作れずSEOが致命的に弱い。ポータルの集客はロングテール検索（「豊洲 子供 英語」等）が生命線 |
| Next.js + Vercel | ✕ | 機能過剰。**Vercel Hobbyプランは商用利用が規約違反**であり、掲載料モデルの本サイトでは採用不可。Proは月$20 |
| **Astro + Cloudflare Pages（採用）** | ◎ | ビルド時に全教室ページを静的HTML化→SEO最強・表示爆速。CF Pagesは**商用利用OK・帯域無制限・ビルド月500回無料**（10日毎なら月3回しか使わない）。サーバ不在＝落ちない・管理不要 |

## 2. データ保持

| 候補 | 評価 | 理由 |
|---|---|---|
| Supabase無料枠 | ✕ | **1週間アクセスがないとプロジェクトが一時停止**され、復旧に人手が要る＝「運用手間ゼロ」に反する。可動部（DBサーバ）が増えるだけで、数百件規模のデータにDBは不要 |
| Cloudflare KV / D1 | △ | 無料枠は十分だが、読み書きにWorkerコードが必要になり、ビルド時のデータ取得・履歴管理が複雑化 |
| **リポジトリ内JSON（採用）** | ◎ | データがGitで全履歴管理され、クローラが変なデータを入れても`git revert`一発で復旧。クローラのcommitがそのままデプロイのトリガーになり、**「データ更新→再ビルド→公開」が1本のパイプラインに統合**される。可動部ゼロ・0円 |

数千件を超えたら同じ構成のままJSONを分割（エリア別ファイル）すれば足りる。DBへの移行が必要になる頃には売上があるはず。

## 3. 10日毎の定期クローラ実行環境

| 候補 | 評価 | 理由 |
|---|---|---|
| Cloudflare Workers Cron | ✕ | 無料枠は**1リクエストCPU 10ms**制限。複数ページの取得+LLM整形のような長時間バッチに不向き。さらにGitHubへの書き戻しにAPI実装が必要 |
| Vercel Cron | ✕ | Hobbyは1日1回まで＆実行10秒制限＆商用不可 |
| **GitHub Actions（採用）** | ◎ | **パブリックリポジトリなら完全無料・無制限**。Node実行環境フル装備、リポジトリへのcommit権限が標準で付き、cron構文で定期実行も内蔵。クローラに最適 |

## 4. AI整形・問い合わせフォーム

- **Gemini Flash系 無料枠**：1日あたりの無料リクエスト枠が十分にあり、10日に1回・数十件の分類+紹介文生成なら**月0円**。キーはGitHub Secretsに保管
- **Formspree無料枠（月50件）**：有料パートナー申込・修正/削除依頼の受付に十分。月50件を超える申込が来たら、それは年商50万円超の嬉しい悲鳴

## 採用構成が「費用0・完全自動・運用最楽」な理由（要約）

1. **可動部がゼロ**：DBなし、サーバなし、API層なし。動的に動くのは10日に1回のActionsだけ。壊れる場所が構造的に存在しない
2. **データ更新とデプロイが1本化**：crawl→commit→自動ビルド→公開。人間の出番なし
3. **全てGit管理**：AIの誤判定もrevertで即復旧。バックアップという概念すら不要
4. **規約面で安全に商用化できる**無料枠だけで構成（CF Pages・GitHub・Gemini・Formspree）
